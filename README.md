# AI Fitness & Pose Detection App

This app uses MediaPipe and OpenCV to detect human poses and count repetitions for exercises like push-ups.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```